{
  "name": "@autonomynexus/accounting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/autonomynexus/accounting.git"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "prepublishOnly": "bun run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "oxlint --fix",
    "format": "oxfmt --write src/ test/"
  },
  "dependencies": {
    "effect": "^3.0.0",
    "date-fns": "^4.0.0"
  },
  "peerDependencies": {
    "monetary": ">=2.0.0"
  },
  "devDependencies": {
    "oxfmt": "^0.31.0",
    "oxlint": "^1.46.0",
    "monetary": "file:../monetary",
    "typescript": "^5.5.0"
  }
}